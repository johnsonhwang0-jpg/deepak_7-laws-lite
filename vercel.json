{
  "buildCommand": "npm install && npm run build",
  "outputDirectory": "build",
  "devCommand": "vite",
  "installCommand": "npm install",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/$1"
    }
  ]
}
